{
    "event": "pending.accepted",
    "method": "POST",
    "url": "https://api.resend.com/emails",
    "headers": {
        "Authorization": "Bearer <RESEND_API_KEY>",
        "Content-Type": "application/json"
    },
    "query_params": {},
    "cookies": {},
    "body": {
        "from": "<SENDER_EMAIL>",
        "to": ["{{email}}"],
        "subject": "Access Granted — {{service}}",
        "text": "Hello {{name}},{{nl}}{{nl}}Your access request for {{service}} has been approved.{{nl}}{{nl}}Access expires on {{expiry_date}} at {{expiry_time}}.{{nl}}{{nl}}— {{owner_name}}"
    }
}
